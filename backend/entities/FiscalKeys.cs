namespace Pims.Dal.Entities
{
    /// <summary>
    /// FiscalKeys enum, provides a list of valid keys for fiscal values.
    /// </summary>
    public enum FiscalKeys
    {
        NetBook,
        Estimated
    }
}
