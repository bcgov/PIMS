namespace Pims.Dal.Entities
{
    /// <summary>
    /// NotificationStatus enum, provides notification status options.
    /// </summary>
    public enum NotificationStatus
    {
        Accepted = 0,
        Pending = 1,
        Cancelled = 2,
        Failed = 3,
        Completed = 4
    }
}
