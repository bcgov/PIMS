namespace Pims.Dal.Entities
{
    /// <summary>
    /// ReportTypes enum, provides a strongly typed wrapper on the valid project report types that can be used.
    /// </summary>
    public enum ReportTypes
    {
        SPL = 0,
    }
}
